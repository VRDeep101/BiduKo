"use client";
import { useState } from "react";
import { ArrowUpRight } from "lucide-react";

interface ContactFormData { name: string; email: string; phone: string; brand: string; need: string; details: string }
interface ContactFormProps { }
export function ContactForm({}: ContactFormProps) { const [sent, setSent] = useState(false); const [form, setForm] = useState<ContactFormData>({ name:"",email:"",phone:"",brand:"",need:"",details:"" }); const update=(key:keyof ContactFormData,value:string)=>setForm({...form,[key]:value}); return <div className="contact-form">{sent ? <div className="sent-state"><span>Brief received</span><h2>Let’s make<br/><em>something move.</em></h2><p>Your brief is ready. Connect your form backend later to send this data to your inbox/CRM.</p></div> : <form onSubmit={e=>{e.preventDefault();setSent(true)}}><div className="form-row"><label>Name<input required value={form.name} onChange={e=>update("name",e.target.value)} placeholder="Your name"/></label><label>Email<input required type="email" value={form.email} onChange={e=>update("email",e.target.value)} placeholder="you@company.com"/></label></div><div className="form-row"><label>Phone<input required value={form.phone} onChange={e=>update("phone",e.target.value)} placeholder="+91 ..."/></label><label>Brand <span>(optional)</span><input value={form.brand} onChange={e=>update("brand",e.target.value)} placeholder="Brand / company"/></label></div><label>What do you need?<input required value={form.need} onChange={e=>update("need",e.target.value)} placeholder="Website, e-commerce, platform..."/></label><label>Project details<textarea required rows={6} value={form.details} onChange={e=>update("details",e.target.value)} placeholder="Tell us what you are building and what success looks like."/></label><button className="cta-button" type="submit">Send project brief <ArrowUpRight size={17}/></button></form>}</div>; }
